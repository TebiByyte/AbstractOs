#include "usb.h"

void usb_get_controller(){
    /*for (int i = 0; i < pci_device_count; i++){
        if (pci_devices[i].class_code == USB_CONTROLLER_CLASS){
            usb_pci_controller.device = &pci_devices[i];
            break;
        }
    }*/
}